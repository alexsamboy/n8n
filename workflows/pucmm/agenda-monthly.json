{
  "id": "848f824105bbf077",
  "name": "Agenda PUCMM — monthly",
  "description": "Programa el boletín mensual de Agenda PUCMM y coordina el envío interno y la campaña de Aliados en Brevo mediante el workflow central.",
  "nodes": [
    {
      "id": "agenda-001",
      "name": "Horario (inactivo al importar)",
      "type": "n8n-nodes-base.scheduleTrigger",
      "typeVersion": 1.2,
      "position": [
        -520,
        -100
      ],
      "parameters": {
        "rule": {
          "interval": [
            {
              "field": "cronExpression",
              "expression": "0 8 * * 1"
            }
          ]
        }
      }
    },
    {
      "id": "agenda-002",
      "name": "Prueba manual con referenceDate",
      "type": "n8n-nodes-base.manualTrigger",
      "typeVersion": 1,
      "position": [
        -520,
        100
      ],
      "parameters": {}
    },
    {
      "id": "agenda-003",
      "name": "Calcular ventana monthly",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        -260,
        0
      ],
      "parameters": {
        "jsCode": "const ZONE = 'America/Santo_Domingo';\nconst OFFSET = '-04:00';\nconst TEST_REFERENCE_DATE = '2026-06-01T12:00:00-04:00';\nconst pad = n => String(n).padStart(2, '0');\nconst reference = $json.referenceDate ? new Date($json.referenceDate) : new Date($execution.mode === 'test' ? TEST_REFERENCE_DATE : Date.now());\nif (Number.isNaN(reference.valueOf())) throw new Error('referenceDate inválida');\nconst parts = Object.fromEntries(new Intl.DateTimeFormat('en-CA',{timeZone:ZONE,year:'numeric',month:'2-digit',day:'2-digit'}).formatToParts(reference).map(p=>[p.type,p.value]));\nconst today = {year:+parts.year,month:+parts.month,day:+parts.day};\nconst shift = (p,d) => { const x=new Date(Date.UTC(p.year,p.month-1,p.day+d)); return {year:x.getUTCFullYear(),month:x.getUTCMonth()+1,day:x.getUTCDate()}; };\nconst iso = p => p.year+'-'+pad(p.month)+'-'+pad(p.day)+'T00:00:00'+OFFSET;\nconst dow = new Date(Date.UTC(today.year,today.month-1,today.day)).getUTCDay();\nlet digestType='none', start, end;\nif (dow===1 && today.day<=7) { digestType='monthly'; start={year:today.year,month:today.month,day:1}; end=today.month===12?{year:today.year+1,month:1,day:1}:{year:today.year,month:today.month+1,day:1}; }\nelse if (dow===1) { digestType='weekly'; start=today; end=shift(today,7); }\nelse if (dow>=2 && dow<=6) { digestType='daily'; start=today; end=shift(today,1); }\nif (digestType !== 'monthly') return [];\nreturn [{json:{digestType,windowStart:iso(start),windowEndExclusive:iso(end),referenceDate:reference.toISOString(),recipientGroup:digestType,timezone:ZONE}}];"
      }
    },
    {
      "id": "agenda-004",
      "name": "Ejecutar build-and-send",
      "type": "n8n-nodes-base.executeWorkflow",
      "typeVersion": 1.3,
      "position": [
        20,
        0
      ],
      "parameters": {
        "workflowId": {
          "__rl": true,
          "mode": "id",
          "value": "81aa01934460cec1"
        },
        "options": {
          "waitForSubWorkflow": true
        }
      }
    }
  ],
  "connections": {
    "Horario (inactivo al importar)": {
      "main": [
        [
          {
            "node": "Calcular ventana monthly",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Prueba manual con referenceDate": {
      "main": [
        [
          {
            "node": "Calcular ventana monthly",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Calcular ventana monthly": {
      "main": [
        [
          {
            "node": "Ejecutar build-and-send",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "pinData": {},
  "active": false,
  "settings": {
    "executionOrder": "v1",
    "timezone": "America/Santo_Domingo",
    "saveDataSuccessExecution": "none",
    "saveDataErrorExecution": "all",
    "saveManualExecutions": false,
    "errorWorkflow": "cf9eb8c32404623b",
    "availableInMCP": true
  },
  "versionId": "a071eca8-0e20-4c25-92f0-e5cfa9bcd7da",
  "meta": {
    "templateCredsSetupCompleted": false
  },
  "tags": [
    {
      "id": "agenda-pucmm",
      "name": "Agenda PUCMM"
    }
  ]
}
